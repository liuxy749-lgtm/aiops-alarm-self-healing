# 变更日志

## 2026-09-16

- **修复恢复探测的 PromQL 选择器**：`up{instance=~"^<ip>:"}` 在 PromQL 中因正则两端自动锚定，永远匹配不到
  `ip:port` 形态的实例，导致依赖该判据的工单恒为「无法确认」而不会自动关闭。
  改为 `up{instance=~"^<ip>(:.*)?"}`。
- **修复非节点类告警的实体节点为空**：实体节点取值的优先级调整为
  「事件显式字段 → 标签（node/nodename/kubernetes_node/Hostname）→ target_ident/hostname 兜底」。
  此前某些规则把 `target_ident`（形如 `ip:port`）当成了节点。
- **恢复判据按实体类型区分**：节点类用 `kube_node_status_condition`；Pod/GPU 等挂在节点上的实体优先用采集端
  `up{}`（节点就绪不代表其上设备正常），无 IP 可用时才退回节点状态并在证据中标注。

## 2026-09-15

- **新增主动恢复探测**：告警静默一段时间后按间隔主动查询权威判据，探测到恢复即关单并推送恢复卡；
  未配置指标源时不探测。
- **新增每日汇总**：每天固定时点推送未恢复工单清单（含当前疑似根因、置信度、持续时长）；
  无未恢复工单时默认不发。
- **卡片按天封顶**：同一工单每天只推第一张卡片，恢复卡不受限。
- **同故障收敛**：同指纹告警在复发窗口内挂回原工单（必要时重新打开），不再按重发周期重复建单。
- **新增 Web 工单页** `/ui/incidents`（服务端渲染，点开就地展开），飞书卡片增加跳转按钮。
- **修复**：告警过期窗口调整为大于告警源重发周期；补齐事件通道推送记录的 `flush()`；
  4 处 PromQL 标签拼接改为统一转义；`up{}` 正则补转义。
- **修复工单号分配**：序号改为单调递增（并入推送留档与时间线里已用过的号段），避免删除工单后复用号段
  与历史推送记录冲突，导致新工单的卡片被幂等逻辑跳过。

## 2026-09-14

- **修复丢事件**：接收端写库冲突时改为「回滚最外层事务后重开并重试」，事务以 `BEGIN IMMEDIATE` 起步；
  同一事件重试期间归档只写一次；新增按事件 ID 记录的重试指标。此前用保存点内层重试不换快照，重试必然失败。
- **修复误判恢复**：告警的实体 IP 不再取集群级采集端地址（如 kube-state-metrics 的 service 地址），
  以 `kube_node_info.internal_ip` 为准；恢复校验改为三态结论（已恢复 / 未恢复 / 无法确认），
  查不到序列时不再判为已恢复；告警静默不再自动关闭工单。
- **新增归档补录工具** `tools/backfill_raw_events.py`：把「归档有原文、库内缺失」的事件重投回入口，
  默认只报告，`--apply` 才真投（服务端幂等）。
- **卡片调整**：标题改为「级别 + 短故障标题」；合并工单新增「节点详情」段落列出全部机器；
  时间统一为本地时区；时间线补齐模型分析结论。
- **指标口径**：飞书干跑不再计入发送成功计数，另设 dry-run 计数。

## 2026-09-11

- 一期交付：事件接收与归档、告警去重、关联引擎、工单生命周期、上下文采集、模型诊断、飞书通知、指标与健康检查。
- 接收路径改为「同步只做入库与关联，副作用出锁」：上下文采集、模型诊断、推送移到后台单线程 worker，
  webhook 由十几秒降到百毫秒级。
- 两轮代码评审，修复 40 余项缺陷；测试覆盖端到端链路与关键边界。
